import pygame
pygame.init()
screen_sleepy=pygame.display.set_mode((540,540))
screen_sleepy.fill("yellow")
done=False
while not done:
  for event in pygame.event.get():
    if event.type==pygame.QUIT:
      pygame.quit()
  pygame.display.flip()
